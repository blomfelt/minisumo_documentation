The home of every variant of the minisumoplusplus bot. 

Each variant get their own folder, which contain both the code and any neccessary 3d-printing files.

Symlinks to other folders are permitted, if they for example use the same chassis as another bot.
A symlink is created in linux / WSL using the `ln -s source_file symbolic_link` command.
